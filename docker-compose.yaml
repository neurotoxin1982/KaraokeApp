version: '3.8'

services:
  web:
    build: .
    restart: always
    ports:
      - "5000:5000"
    volumes:
      - video_data:/app/video
      - yt_home:/app/yt_home

volumes:
  video_data:
  yt_home:
